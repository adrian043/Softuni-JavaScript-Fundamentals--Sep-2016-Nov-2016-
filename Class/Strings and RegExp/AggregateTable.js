function aggTable(input) {
    // let oneRow = input.join('').split(/[ |]+/).filter(x => x != '').join(' ');
    // let textOnly = oneRow.split(/ [0-9]+/).filter(x => x != '').join(',');
    // let numz = oneRow.split(/ *[A-z][a-z]+/).filter(x => x != '').join('').trim();
    // let arrOfNumz = numz.split(' ').map(Number).reduce((a,b) => a+b);
    //
    // console.log(textOnly);
    // console.log(arrOfNumz);
}

aggTable(
    ['| SOFIA asdasdasd        asd   | 300',
        '| Veliko Tarnovo  sdsd| 500',
        '| Yambol asdasdasd         sdsd| 275']

);